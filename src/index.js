import React from 'react';
import ReactDOM from 'react-dom';
import './app/api/globals.css';
import App from './app';

ReactDOM.createRoot(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

